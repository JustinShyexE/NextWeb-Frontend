import { Comic_Neue } from '@next/font/google';
import {Inria_Sans}from '@next/font/google';
import {Lilita_One}from '@next/font/google';
import {Abhaya_Libre} from '@next/font/google';
import {Noto_Sans} from '@next/font/google';

export const comicNeue = Comic_Neue({ 
    subsets: ['latin'],
    weight: ["400"]
});

export const  irinaSans = Inria_Sans({ 
    subsets: ['latin'],
    weight: ["700"]
});

export const  LilitaOne = Lilita_One({ 
    subsets: ['latin'],
    weight: ["400"]
});

export const AbhayaLibre = Abhaya_Libre({
    subsets: ['latin'],
    weight: ["700"]
})

export const AbhayaLibre2 = Abhaya_Libre({
    subsets: ['latin'],
    weight: ["800"],
    
})
